module Caculator

  def Caculator.sum(a,b)
    puts "Sum is #{a+b}"
  end
  def Caculator.prod(a,b)
    puts "prodcut is #{a*b}"
  end

  def Caculator.mycal(a,b,c)
    puts "my cal is #{a*b + c }"
  end
end
